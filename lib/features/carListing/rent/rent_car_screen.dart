import 'package:flutter/material.dart';

class RentCarScreen extends StatelessWidget {
  const RentCarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('RentCar');
  }
}
