import 'package:flutter/material.dart';

class SearchByScreen extends StatelessWidget {
  const SearchByScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('SearchByScreen');
  }
}
