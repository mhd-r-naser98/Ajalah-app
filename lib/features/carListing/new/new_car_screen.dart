import 'package:flutter/material.dart';

class NewCarScreen extends StatelessWidget {
  const NewCarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('NewCarScreen');
  }
}
