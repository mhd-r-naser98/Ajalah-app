import 'package:flutter/material.dart';

class ImportedScreen extends StatelessWidget {
  const ImportedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('ImportedScreen');
  }
}
