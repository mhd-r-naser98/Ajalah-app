import 'package:flutter/material.dart';

class UsedScreen extends StatelessWidget {
  const UsedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('UsedScreen');
  }
}
